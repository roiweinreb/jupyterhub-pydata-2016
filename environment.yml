name: jupyterhub-tutorial
channels:
  - conda-forge
dependencies:
  - python == 3.5
  - jupyterhub == 0.6.1
  - notebook >= 4.2
  - ipykernel >= 4.3
  - pandas-gbq >= 0.9.0
  - pip:
    - oauthenticator
    - dockerspawner
    - netifaces
